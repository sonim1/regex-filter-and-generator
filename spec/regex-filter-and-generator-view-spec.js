'use babel';

import RegexFilterAndGeneratorView from '../lib/regex-filter-and-generator-view';

describe('RegexFilterAndGeneratorView', () => {
  // it('has one valid test', () => {
  //   expect('life').toBe('easy');
  // });
});
